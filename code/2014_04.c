#include <stdio.h>

/*
˼·: �ҵ�Ӧ�ò����λ��,Ȼ�������������ƶ�
�Ҳ����λ�ò������ 

*/

int main()
{
	int a[11] = {1, 4, 6, 9, 13, 16, 19, 28, 40, 45}; // ��һ��λ��,���һ��λ��Ϊ0
	int x;
	scanf("%d", &x);
	int i = 0; // Ҫ�����λ��
	while (i < 10 && a[i] <= x)
		i++; // iͣ������ʱ��a[i]>x ���� ���˱߽�
	if (i == 10)
		a[10] = x;
	else
	{ // ���� �±�[i,9]����Ҫ����ƶ�һλ
		int j;
		for (j = 10; j > i; j--)
		{
			int t = a[j];
			a[j] = a[j - 1];
			a[j - 1] = t;
		}
		a[i] = x;
	}
	for (i = 0; i < 11; i++)
		printf("%d ", a[i]);
	return 0;
}
